{
  "relatedBlocks": [
    "DEMUX - inverse operation (splitter)",
    "MUX_f - floating point multiplexer",
    "SELECT_m - select from multiple inputs"
  ],
  "criticalRules": [
    "exprs[0] determines number of inputs",
    "integerParameters[0] MUST match exprs value",
    "MUST include ALL standard parameter blocks (realParameters, integerParameters, objectsParameters, etc.)",
    "Output is a vector combining all inputs",
    "Width is 10 (very narrow)"
  ],
  "description": "Multiplexer - combines multiple signals into a single vector output",
  "anomalies": [
    "Very narrow width (10) - minimal visual representation",
    "dataLines=0 on output - dimension is computed from inputs",
    "dataLines=-1,-2 on inputs indicates auto-sizing",
    "exprs value must match integerParameters value"
  ],
  "name": "MUX",
  "commonUses": [
    "Combining scalar signals into vectors",
    "Creating multi-channel signals",
    "Preparing data for matrix operations",
    "Connecting to blocks expecting vector inputs"
  ],
  "sourceFile": "scilab-2026.0.1/scilab-2026.0.1/scilab/modules/scicos_blocks/macros/Branching/MUX.sci"
}